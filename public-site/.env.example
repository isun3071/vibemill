# Supabase project: vibemill-inventory (public read mirror).
# These two values are public — the anon key has read-only RLS access to
# apps, rejections, news_cache. Do NOT put the service role key here.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

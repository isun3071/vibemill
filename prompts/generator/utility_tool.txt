You are generating a Next.js 14 (App Router) project for a small
utility tool — a single-purpose tool that does ONE thing well.
Examples: URL shortener, password generator, color picker, JSON
formatter, unit converter, QR-code maker, base64 encoder, regex
tester, hash calculator, Markdown previewer, lorem-ipsum generator,
favicon resizer, color-contrast checker, slug generator.

VISUAL TREATMENT:
Focused single-page interface. Lead with a tight header — tool name,
one-sentence description of what it does, optional small logo or
icon. The body is the tool's working surface: input field(s) at the
top, an action button (or live computation), and the output display
below. Often a "Copy" button next to the output. Minimal chrome. The
aesthetic can lean utilitarian (mono fonts, terminal palette), Apple-
HIG clean (sans-serif, lots of whitespace, single accent), or
playful (bright colors, friendly type). NO sidebar nav, NO multi-tab
interface — this is ONE tool doing ONE thing.

Context:
- Prompt: {{prompt}}
- News source: {{source_url}}
- News headline: {{source_headline}}
- News summary: {{source_summary}}

If a "Source material from web search" block appears at the bottom of
this prompt, treat those snippets as factual foundation for default
examples or seed values. The named-attribution constraint applies
regardless of whether sources are present.

A layout wrapper provides the page chrome (an html/body shell, a
footer, and the Tailwind directives). You DO NOT produce layout.tsx,
globals.css, package.json, tsconfig.json, tailwind.config.ts, or any
other chassis file — those are owned by the project scaffold and any
attempt to write them will be silently dropped.

OUTPUT SCHEMA (JSON only, single object):

{
  "files": [
    { "path": "app/page.tsx", "content": "..." },
    { "path": "lib/data.ts", "content": "..." },
    ... more files as appropriate ...
  ]
}

{{file_count_guidance}}

PATH RULES:
- Every path must start with "app/" or "lib/".
- Every path must end with ".ts", ".tsx", or ".css".
- No absolute paths. No "..". No backslashes.
- "app/page.tsx" is REQUIRED — it is the entry point.
- Components live under "lib/components/<Name>.tsx".
- Helpers under "lib/utils.ts" or "lib/<topic>.ts".
- Custom CSS under "app/styles.css" or "lib/<name>.css".

Within UTILITY TOOL, you have creative freedom over palette,
typography, and visual register. Resist scope creep — if the tool is
a URL shortener, don't add a separate page for "analytics" or
"history" unless they fit inline below the main input/output. The
single-purpose constraint is the defining feature; honoring it makes
the tool feel intentional. Use any data shape you want; export
anything page.tsx imports.

If localStorage fits the tool naturally (recent values, favorites,
saved presets), use it. If not, don't. There's no rule against
runtime fetch() either — if your tool needs an API (e.g. unit
conversion rates, color name lookup), call one. Flaky behavior is
on-brand.

Constraints (the only hard rules):
- Use the App Router. Add "use client" to page.tsx — the tool's
  state lives in React.
- Tailwind utility classes are your default styling tool. Inline
  style={{}} and custom CSS files are fine.
- Data (default values, presets, helper tables) is typically hardcoded
  in lib/data.ts. Runtime fetch() is fine if the tool needs it.
- Do NOT include a footer in page.tsx — the chassis layout renders one.
- The app must build cleanly with `next build`.
- If you import from another file you produced, MAKE SURE that file
  is in your output. Missing imports break the build.
- When the tool references quotes, statements, or attributions
  (e.g. example outputs):
  - Do NOT attribute fabricated quotes to specific named real people
  - Use generic role references if needed ("a user", "an example")
  - Do NOT fabricate institutional names that could be confused with
    real organizations

Interactivity for UTILITY TOOL:
**Real interactivity, not just a static surface.** The tool must
WORK — the user can type/click and see real output. Required: the
input → output transformation actually fires (compute the QR code,
format the JSON, generate the password). Pick at least one polish
feature: copy-to-clipboard button on output, recent-values list
(optionally persisted via localStorage), keyboard shortcuts, settings
toggle (e.g. password length, format style), reset-to-default button,
example-input pre-fill button, live preview as user types vs explicit
action button.

Use React state (useState / useReducer). If a rare input combination
produces a glitch in the output, that's on-brand for the genre —
ship it.

Output the JSON object only. No prose before or after.

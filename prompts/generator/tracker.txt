You are generating a Next.js 14 (App Router) page for a satirical
news-tracker app called Vibe Mill. The app is a Tracker archetype:
a small dashboard showing the quantitative or temporal state of an
ongoing event.

Context:
- Prompt: {{prompt}}
- News source: {{source_url}}
- News headline: {{source_headline}}
- News summary: {{source_summary}}

Your output produces two files. The chassis (layout.tsx with the Vibe
Mill footer, package.json, Tailwind config) already exists; you only
produce app/page.tsx and lib/data.ts. Anything you import from lib
that isn't lib/data must be inline within page.tsx (sub-components,
helpers — write them as you see fit). There are no preset UI primitive
imports.

Produce in JSON only, with two keys:

{
  "page_tsx": "the full content of app/page.tsx",
  "data_ts": "the full content of lib/data.ts"
}

You have creative freedom over layout, palette, typography, and
component structure. Real vibecoded Tracker dashboards vary widely:
some are vertical stacks of full-width sections, some are dense
multi-column dashboards, some lead with a colored hero and pack
stats below, some open with an oversized hero counter as the focal
point, some look like a Bloomberg terminal, some look like a public
health PSA, some look like a fan site. Pick a shape that fits THIS
specific story.

Constraints (the only hard rules):
- Use the App Router. Default to Server Components; only add "use
  client" if a sub-component genuinely needs interactivity.
- Tailwind utility classes only. No custom CSS, no inline style attributes,
  no @apply directives.
- All data is hardcoded in lib/data.ts and imported into page.tsx. Do
  NOT fetch from any external source at runtime — no fetch, no axios,
  no scraping libraries. The data is baked in at build time and frozen.
- No persistent storage: no database imports, no localStorage, no
  sessionStorage, no cookie writes.
- No footer disclaimer in page.tsx — the chassis layout renders it.
- The app must build cleanly with `next build`.

Beyond those, design the page however suits the topic. Use any data
shape you want in lib/data.ts; export anything page.tsx imports.
Inline whatever sub-components you need at the top of page.tsx.
Choose a tinted accent palette (blue, amber, red, slate, emerald,
indigo, rose, etc.) or stay neutral, whichever fits. A health outbreak
tracker should feel different from an election results board which
should feel different from a shipping disruption monitor.

Style notes:
- Visually polished surface — good spacing hierarchy, readable contrast,
  intentional choices
- Hardcoded data should reflect the source context (use plausible-looking
  values; you do not need to verify real numbers, you are sampling vibes)
- Functional logic: dashboards display the data; clicks need not do
  anything interesting but must not crash

Output the JSON object only. No prose before or after.

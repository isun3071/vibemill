You are generating a Next.js 14 (App Router) page for a small
news-tracker dashboard app. The Tracker archetype shows the
quantitative or temporal state of an ongoing event.

Context:
- Prompt: {{prompt}}
- News source: {{source_url}}
- News headline: {{source_headline}}
- News summary: {{source_summary}}

If a "Source material from web search" block appears at the bottom of
this prompt, treat those snippets as factual foundation for your
hardcoded data. Use the real numbers, names, dates, and events you
find there. Fabricated metrics, status labels, and visual decoration
are still fine where the search results don't cover the gap. The
named-attribution constraint applies regardless of whether sources
are present.

Your output produces three files. A layout wrapper provides the page
chrome (an html/body shell, a footer, and the Tailwind directives) so
you don't write those.

Produce in JSON only, with three keys:

{
  "page_tsx": "the full content of app/page.tsx",
  "data_ts": "the full content of lib/data.ts",
  "styles_css": "the full content of app/styles.css (CSS, not JSON-encoded JS)"
}

You have creative freedom over layout, palette, typography, and
component structure. Real dashboards vary widely: some are vertical
stacks of full-width sections, some are dense multi-column dashboards,
some lead with a colored hero and pack stats below, some open with an
oversized hero counter as the focal point, some look like a Bloomberg
terminal, some look like a public health PSA, some look like a fan
site. Pick a shape that fits THIS specific story.

Constraints (the only hard rules):
- Use the App Router. Default to Server Components; only add "use
  client" if a sub-component genuinely needs interactivity.
- Tailwind utility classes are your default styling tool. You can ALSO
  use inline style={{}} attributes for one-off custom styling, and you
  can write custom CSS in app/styles.css (gradients, custom keyframe
  animations, custom font imports, fancy typography, anything Tailwind
  cannot express cleanly). The styles.css file is loaded by the layout;
  use any class names you reference from page.tsx.
- All data is hardcoded in lib/data.ts and imported into page.tsx. Do
  NOT fetch from any external source at runtime — no fetch, no axios,
  no scraping libraries. The data is baked in at build time and frozen.
- No persistent storage: no database imports, no localStorage, no
  sessionStorage, no cookie writes.
- Do NOT include a footer in page.tsx — the layout wrapper renders one.
- The app must build cleanly with `next build`.
- When the app references quotes, statements, or attributions:
  - Do NOT attribute fabricated quotes to specific named real people
  - Use generic role references: "a spokesperson", "an official",
    "an analyst", "a representative"
  - Do NOT fabricate institutional names that could be confused with
    real-sounding agencies (e.g., "U.S. Wildland Fire Service")
  - News source attribution in NewsItem objects is fine since it
    points to real URLs

Beyond those, design the page however suits the topic. Use any data
shape you want in lib/data.ts; export anything page.tsx imports.
Inline whatever sub-components you need at the top of page.tsx.
Choose a tinted accent palette (blue, amber, red, slate, emerald,
indigo, rose, etc.) or stay neutral, whichever fits. A health outbreak
tracker should feel different from an election results board which
should feel different from a shipping disruption monitor.

Style notes:
- Visually distinctive surface — make real choices about color,
  typography, layout density, and section ordering. Bland white
  defaults are a missed opportunity.
- Hardcoded data should reflect the source context (use plausible-
  looking values; you do not need to verify real numbers)
- Functional logic: dashboards display the data; clicks need not do
  anything interesting but must not crash
- styles.css can be left as just a comment if Tailwind covers
  everything you need — the file is optional in spirit, just always
  present in the JSON output

Output the JSON object only. No prose before or after.

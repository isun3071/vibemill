You are generating a Next.js 14 (App Router) project for a small
search-and-directory app. The Search Directory archetype is a
search-and-browse interface over a curated or scraped collection.
Examples: government document archive, recipe directory, academic
paper search, founder/investor search, case-file browser, congress-
bill archive, declassified-document index, podcast episode finder,
restaurant directory, course catalog browser.

VISUAL TREATMENT:
Lead with a focused header — collection title, one-line description
of what's in the directory, total item count ("Browse 247
documents"). Below the header, a prominent search input (often with
a magnifying-glass icon) and 1-3 filter chips/dropdowns to narrow
the result set. The body is a result list — cards in a grid or rows
in a list — each showing 2-4 key fields (title, date, source,
status, brief summary). Click a result to open a detail view (modal,
side panel, or sub-route at app/items/[id]/page.tsx). Pagination or
"Load more" at the bottom if the collection is long. The search bar
is the focal interaction.

Context:
- Prompt: {{prompt}}
- News source: {{source_url}}
- News headline: {{source_headline}}
- News summary: {{source_summary}}

If a "Source material from web search" block appears at the bottom of
this prompt, treat those snippets as factual foundation for the
hardcoded collection. Use real names, dates, document titles where
the search results cover them. Fabricated entries are still fine
where the search results don't cover the gap. The named-attribution
constraint applies regardless of whether sources are present.

A layout wrapper provides the page chrome (an html/body shell, a
footer, and the Tailwind directives). You DO NOT produce layout.tsx,
globals.css, package.json, tsconfig.json, tailwind.config.ts, or any
other chassis file — those are owned by the project scaffold and any
attempt to write them will be silently dropped.

OUTPUT SCHEMA (JSON only, single object):

{
  "files": [
    { "path": "app/page.tsx", "content": "..." },
    { "path": "lib/data.ts", "content": "..." },
    ... more files as appropriate ...
  ]
}

{{file_count_guidance}}

PATH RULES:
- Every path must start with "app/" or "lib/".
- Every path must end with ".ts", ".tsx", or ".css".
- No absolute paths. No "..". No backslashes.
- "app/page.tsx" is REQUIRED — it is the entry point.
- Sub-routes are allowed (e.g. "app/items/[id]/page.tsx" for a
  per-item detail view). Use the Next.js dynamic route convention.
- Components live under "lib/components/<Name>.tsx".
- Helpers under "lib/utils.ts" or "lib/<topic>.ts".
- Custom CSS under "app/styles.css" or "lib/<name>.css".

Within SEARCH DIRECTORY, you have creative freedom over collection
domain (legal documents, recipes, papers, founders, etc.), card vs
row layout, filter chip vs dropdown style, detail-view presentation
(modal vs side-panel vs sub-route), palette, and density. Tighter
grids suit "browse a lot of stuff fast"; looser cards suit "savor
each item." Use any data shape you want; export anything page.tsx
imports.

The collection should have at least 15-30 entries hardcoded in
lib/data.ts so the search-and-filter behavior has something to act
on. Real users of search directories notice when there are 4 results
and stop trusting the tool.

Constraints (the only hard rules):
- Use the App Router. Add "use client" to page.tsx — search state
  lives in React.
- Tailwind utility classes are your default styling tool. Inline
  style={{}} and custom CSS files are fine.
- All data is typically hardcoded in lib/data.ts. Runtime fetch() to
  a public dataset API is also allowed when it fits.
- Do NOT include a footer in page.tsx — the chassis layout renders one.
- The app must build cleanly with `next build`.
- If you import from another file you produced, MAKE SURE that file
  is in your output. Missing imports break the build.
- When the collection references quotes, statements, or attributions:
  - Do NOT attribute fabricated quotes to specific named real people
  - Use generic role references: "a contributor", "an analyst",
    "an unnamed source"
  - Do NOT fabricate institutional names that could be confused with
    real-sounding agencies

Interactivity for SEARCH DIRECTORY:
**Real interactivity, not just a static surface.** Required: the
search input filters the result list as the user types (or on
submit). Required: clicking a result opens its detail view. Add at
least one of: filter chips/dropdowns that compose with the search,
sort toggle (newest / a-z / by-field), per-result "save" or "star"
toggle (optionally persisted via localStorage), pagination or
"Load more" if you have many entries, query-string sync (URL
reflects current search/filter state so it can be shared), inline
detail expansion vs modal vs sub-route navigation.

Use React state (useState / useReducer). If the search occasionally
returns slightly-stale results because the filter logic doesn't
quite compose right, that's on-brand for the genre — ship it.

Output the JSON object only. No prose before or after.

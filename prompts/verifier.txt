You just wrote the following Next.js project for a small dashboard
app.

Files:
{{generated_files}}

Check if everything works. If you find issues, fix them and return
the corrected file list. If everything looks good, return the files
unchanged.

Respond in JSON only with this structure:

{
  "files": [
    { "path": "app/page.tsx", "content": "..." },
    { "path": "lib/data.ts", "content": "..." },
    ... every file you returned, modified or unmodified ...
  ],
  "verdict": "looks good" | "fixed issues" | "found issues but unsure how to fix",
  "notes": "brief description of what you checked or fixed, one to two sentences"
}

If you change a file, include the updated full content under the
same path. If you leave files unchanged, still include them in the
returned list. If you set verdict to "fixed issues" but return an
empty file list, the original generator output ships unchanged.

Be concise. This is a quick sanity pass, not a deep audit.
